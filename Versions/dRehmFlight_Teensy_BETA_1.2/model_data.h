// Placeholder model_data.h for TensorFlow Lite Micro
// Replace this file with a generated `model_data.h` produced from a real `.tflite` model.
// Use `tools/convert_tflite_to_model_data.py` to convert a .tflite into this header.

#ifndef MODEL_DATA_H
#define MODEL_DATA_H

#include <stdint.h>

// Minimal dummy buffer (NOT a valid TFLite flatbuffer).
// This placeholder allows the project to compile; do NOT enable USE_TFLM
// at build time unless you replace this with a valid TFLite model.
static const unsigned char model_data[] = { 0x20, 0x00, 0x00, 0x00 };
static const unsigned int model_data_len = sizeof(model_data);

#endif // MODEL_DATA_H
