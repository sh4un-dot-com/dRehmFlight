// Top-level wrapper to ensure PlatformIO converts the Arduino sketch correctly.
// This simple file includes the original sketch so the Arduino build system
// produces a proper `setup()`/`loop()` translation unit.
#include "dRehmFlight_Teensy_BETA_1.2.ino"
